# Build
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
ARM64/
[Ll]og/
[Ll]ogs/
artifacts/
publish/
*.binlog

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json

# NuGet
*.nupkg
*.snupkg
packages/
!packages/build/
project.lock.json
project.fragment.lock.json

# ==================== fora do repositório público ====================
# O PRD é o documento de projeto: roteiro, decisões e o registro de armadilhas.
# Fica só na máquina. O arquivo continua existindo em disco; o que muda é que o
# git deixa de segui-lo. Os links para ele foram removidos do README e do
# CONTRIBUTING — reponha-os se algum dia voltar.
/PRD.md

# Contexto para o Claude Code. Ficam só na máquina, os dois: o CLAUDE.md com as
# convenções e armadilhas do projeto, o CLAUDE.local.md com o estado do momento.
# Continuam carregando normalmente — o Claude Code lê pelo caminho, não pelo git.
/CLAUDE.md
/CLAUDE.local.md

# Atalhos de sessão desta máquina. O CLAUDE-ELEVADO.bat carrega o caminho absoluto
# da pasta do autor dentro dele — é configuração de quem roda, não do projeto.
/CLAUDE-*.bat
/CLAUDE-*.cmd

# Rascunhos de sessão (scripts de manifesto, saídas coladas, testes de uma vez só).
scratchpad/

# Snapshots binários do próprio Vacuon. Guardam o array de FileEntry inteiro e o blob
# de nomes — ou seja, a árvore de arquivos de quem varreu. Nunca entram no repositório.
*.vsnap

# A suíte NÃO entra aqui, e é deliberado. Ela é a evidência de tudo que o README
# afirma sobre o parser de MFT, e "compile e teste você mesmo" é a alternativa
# que o projeto oferece a quem não quer confiar num binário sem assinatura.
# O que fica de fora é só o lixo que a execução produz.

# ==================== artefatos de teste ====================

# resultados de execução
[Tt]est[Rr]esult*/
TestResults/
*.trx
*.testlog
*.VisualState.xml
*.receipt
test-output*.xml
junit*.xml

# cobertura
coverage*.json
coverage*.xml
coverage*.info
coverage*.cobertura.xml
coverage*.opencover.xml
*.coverage
*.coveragexml
lcov.info
.coverlet/
CoverletOutput*/
[Cc]overage[Rr]eport*/

# runners e caches
.vstest/
*.vsp
*.vspx
*.sap
BenchmarkDotNet.Artifacts/
.benchmarks/

# imagens VHD sintéticas geradas em tempo de teste
tests/**/fixtures/generated/
tests/**/*.vhd
tests/**/*.vhdx

# Saídas do próprio Vacuon
*.thumb.bmp
snapshots/
rules/user.json
config.local.ini

# Sistema
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.tmp
*~
